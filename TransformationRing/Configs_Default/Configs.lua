TransformationRing.Active = true

TransformationRing.Ring[13*512+10] = {
    Skin = 5,
    CustomMessage = {        
        Enable = true,
        Level = 1, -- notice level (0,1,3)
        Text = {
            "[TEXT] Custom Message Eng",
            "[TEXT] Custom Message Por",
            "[TEXT] Custom Message Esp",
        }  
    }
}