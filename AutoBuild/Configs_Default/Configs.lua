AutoBuild.Switch = true -- true --> on | false --> off

AutoBuild.Command = {
    Number = 200
}

AutoBuild.Strings = {
    [0] = {
        "[SYSTEM] There are no Custom Builds for your class", --ENG
        "[SYSTEM] Não existem Custom Builds para sua classe", --POR
        "[SYSTEM] No existem Custom Builds para su clase", --SPN
        1 -- MESSAGE LEVEL (0,1 OR 3)
    },
    [1] = {
        "[SYSTEM] Build number not found", --ENG
        "[SYSTEM] Número da build não encontrado", --POR
        "[SYSTEM] Número de build no existe", --SPN
        1 -- MESSAGE LEVEL (0,1 OR 3)
    },
    [2] = {
        "[SYSTEM] Build %d done with success", --ENG
        "[SYSTEM] Build %d realizada com sucesso", --POR
        "[SYSTEM] Build %d realizada con éxito", --SPN
        1 -- MESSAGE LEVEL (0,1 OR 3)
    },
}